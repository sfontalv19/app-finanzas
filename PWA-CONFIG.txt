// CONFIGURACIÓN DE PWA - APLICAR DESPUÉS DE INSTALAR vite-plugin-pwa
//
// PASOS:
// 1. Detener el servidor (Ctrl+C)
// 2. Instalar: npm install -D vite-plugin-pwa
// 3. Reemplazar el contenido COMPLETO de /vite.config.ts con el código de abajo
// 4. Volver a correr: npm run dev
//
// ====================================================================
// COPIAR DESDE AQUÍ HASTA EL FINAL DE ESTE ARCHIVO A /vite.config.ts
// ====================================================================

import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg'],
      manifest: {
        name: 'Finanzas App',
        short_name: 'Finanzas',
        description: 'Controla tus finanzas personales con estilo',
        theme_color: '#A78BFA',
        background_color: '#FAFAFA',
        display: 'standalone',
        orientation: 'portrait',
        scope: '/',
        start_url: '/',
        lang: 'es-CO',
        icons: [
          {
            src: 'favicon.svg',
            sizes: '192x192',
            type: 'image/svg+xml',
            purpose: 'any maskable',
          },
          {
            src: 'favicon.svg',
            sizes: '512x512',
            type: 'image/svg+xml',
            purpose: 'any maskable',
          },
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,svg,png,ico}'],
        cleanupOutdatedCaches: true,
        skipWaiting: true,
        clientsClaim: true,
      },
      devOptions: {
        enabled: false,
      },
    }),
  ],
})
